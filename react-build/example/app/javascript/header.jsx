var React = require('react');
var ReactDOM = require('react-dom');

module.exports = React.createClass({
    render: function() {
        return (
            <header>
                <h1>Welcome to React Lotto!</h1>
                <h2>The lotto where you dream less, but the mathematical expectation of your gains is much higher!</h2>
            </header>
        );
    }
});
